
public class Main {
    public static void main(String[] args) {
        UserInterface ui = new UserInterface(); //Creates a new instance of the UserInterface class called ui
        ui.testerAdds(); //Calls the testerAdds method from the UserInterface class to add some test films to our collection
        ui.startProgram(); //Starts the program via the UserInterface class
    }
}
